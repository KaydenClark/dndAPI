# Local Compendium Source

Place local compendium source files in `vendor/5etools-data/`.

This folder is kept out of git on purpose. The application can seed from the local vendored copy without depending on an external drive path, but the third-party source files themselves are not published with the repository.

